[user]
	name = Shitikov Kirill
	email = k.shitikov@itransition.com
[credential]
    helper = cache --timeout=3600
[core]
	autocrlf = input
	excludesfile = ~/.gitignore
	editor = nano
[color]
	ui = true
[url "ssh://git@github.com/"]
	insteadOf = https://github.com/
[url "ssh://gitlab@gitlab.server/"]
	insteadOf = http://gitlab.server/
[url "ssh://git@lab.thinkglobal.space/"]
	insteadOf = https://lab.thinkglobal.space/
[alias]
	ct = checkout
	br = branch
	co = commit
	st = status
	hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
	fhist = log --follow -p --
	unstage = reset HEAD --
	last = log -1 HEAD
	target = git log --oneline --decorate
	gitg = !gitg
	cleanup = "!git branch --merged | grep  -v '\\*\\|master\\|develop' | xargs -n 1 git branch -d"
	sdiff = '!'"git diff && git submodule foreach 'git diff'"
	spush = "push --recurse-submodules=on-demand"
	supdate = "submodule update --remoute --merge"
